#include <stdio.h>
int main() {
    // int n;
    // scanf("%d", &n);
    // int a[n];
    int x=10;
    int a[4]={10};
    // for (int i = 0; i < n; i++)
    // {
    //     scanf("%d", &a[i]);
    // }
    for (int i = 0; i < 4; i++)
    {
        printf("%d ", a[i]);
    }
    
    return 0;
}